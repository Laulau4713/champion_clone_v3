# Champion Clone MVP - Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# AI Services (Choose ONE option)
# ===========================================

# Option 1: FREE - Groq API (Recommended for testing)
# Get your FREE API key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_your-groq-api-key

# Option 2: Paid - Anthropic Claude (Production)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# ===========================================
# Transcription (Automatic - No API Key Needed!)
# ===========================================

# Uses LOCAL Whisper model (FREE) - No OpenAI API key required!
# Model sizes: tiny, base, small, medium, large
# Smaller = faster, Larger = more accurate
WHISPER_MODEL=base

# Optional: OpenAI Whisper API (if you prefer cloud)
# OPENAI_API_KEY=sk-your-openai-api-key

# ===========================================
# Model Configuration
# ===========================================

# Groq model (FREE)
GROQ_MODEL=llama-3.1-70b-versatile

# Claude models (if using Anthropic)
CLAUDE_OPUS_MODEL=claude-opus-4-20250514
CLAUDE_SONNET_MODEL=claude-sonnet-4-20250514

# ===========================================
# Database Configuration
# ===========================================

# PostgreSQL (Production)
# DATABASE_URL=postgresql://user:password@localhost:5432/champion_clone

# SQLite (Development) - Leave empty to use SQLite
DATABASE_URL=

# ===========================================
# Server Configuration
# ===========================================

# Server host and port
HOST=0.0.0.0
PORT=8000

# Enable debug mode (enables auto-reload and verbose errors)
DEBUG=true

# ===========================================
# File Storage
# ===========================================

# Directory for uploaded videos
UPLOAD_DIR=./uploads

# Directory for extracted audio files
AUDIO_DIR=./audio

# ===========================================
# Security
# ===========================================

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# JWT Secret Key - REQUIRED in production!
# Generate with: openssl rand -hex 32
# In DEBUG mode, a default is used. In production, app will NOT start without this.
JWT_SECRET=

# Refresh Token Secret (different from access token secret)
# Generate with: openssl rand -hex 32
REFRESH_TOKEN_SECRET=

# ===========================================
# Admin User Configuration
# ===========================================

# Admin email (default: admin@championclone.dev)
ADMIN_EMAIL=admin@championclone.dev

# Admin password - REQUIRED in production!
# Must meet security requirements: 8+ chars, uppercase, lowercase, digit, special char
# In DEBUG mode, defaults to "Admin123!" if not set
ADMIN_PASSWORD=

# Admin display name
ADMIN_NAME=Administrateur

# ===========================================
# Email / SMTP Configuration (Optional)
# ===========================================

# SMTP server settings
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=true
SMTP_FROM=noreply@championclone.dev

# ===========================================
# Optional Services
# ===========================================

# ElevenLabs (voice cloning)
# ELEVENLABS_API_KEY=your-elevenlabs-key

# Qdrant (vector database for pattern search)
# QDRANT_URL=http://localhost:6333

# Redis (session persistence)
# REDIS_URL=redis://localhost:6379
